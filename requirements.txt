# =============================================================================
# requirements.txt — Speech-to-Text API
#
# NOTE: WhisperX is NOT listed here because it must be installed AFTER torch
# with --no-deps to prevent pip from downgrading/changing the torch version.
#
# Install command (run AFTER all deps below are installed):
#   pip install --no-deps git+https://github.com/m-bain/whisperX.git@v3.8.5
#
# Colab users: run the notebook cell in speech2text_colab.ipynb instead —
#   it handles the correct install order automatically.
# =============================================================================

# --- Core Framework ---
# Ranges allow coexistence with Colab's pre-installed google-adk / gradio.
fastapi>=0.124.1,<2.0.0
uvicorn[standard]>=0.34.0,<2.0.0
starlette>=0.49.1,<2.0.0
python-multipart>=0.0.18

# --- Queue & Cache ---
redis>=5.2.0,<6.0.0
rq>=2.0.0,<3.0.0

# --- Audio Processing ---
# faster-whisper 1.2+ is required by whisperx v3.8.5
faster-whisper>=1.2.0
# ctranslate2 4.5+ required by whisperx v3.8.5
ctranslate2>=4.5.0

# Silero-VAD (speech detection before chunking)
silero-vad>=5.1.2

pydub>=0.25.1
soundfile>=0.12.1
librosa>=0.10.2

# --- Speaker Diarization ---
# pyannote.audio 4.x is required by whisperx v3.8.5
pyannote.audio>=4.0.0
pyannote.core>=5.0.0
pyannote.pipeline>=3.0.1

# --- WhisperX runtime deps (installed without whisperx to avoid torch conflicts) ---
pandas>=2.2.3
nltk>=3.9.1
omegaconf>=2.3.0

# --- Hugging Face ---
# >=0.33.5 required by gradio/diffusers; <1.0.0 avoids breaking API changes
huggingface-hub>=0.33.5,<1.0.0
transformers>=4.48.0

# --- Configuration ---
pyyaml>=6.0.2

# --- HTTP ---
# >=0.28.1 required by google-genai / firebase-admin in Colab
httpx>=0.28.1
aiofiles>=24.1.0

# --- Utilities ---
# numpy 2.x is required by whisperx v3.8.5 and Colab's cupy/jax packages.
# Do NOT pin to 1.x — it breaks half of Colab's pre-installed packages.
numpy>=2.1.0
# torch and torchaudio: installed separately via PyTorch index (see colab_setup.sh).
# Listed here for reference only — do not change these to match what's in Colab.
torch>=2.5.1
torchaudio>=2.5.1

# --- Logging ---
loguru>=0.7.3
